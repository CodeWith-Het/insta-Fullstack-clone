user={
    username:string,  
    password:string,
    email:string,
    bio:string,
    profile_image:string  
}

Authenication:
registor(1. user ka data save karna 2. user ko token dena)
login
log out
[OTP based registration]

POST
create
can see the feed
like post
save post

users
following
followers

POST: /api/auth/login
user login with username or email 